#include <iostream>
#include <string>
using namespace std;

int main()
{
    int n;
    string s;
    cin >> n;
    s = to_string(n);
    for (int i = 0; i < s.size(); ++i)
        cout << s[i] << " ";
    return 0;
}
